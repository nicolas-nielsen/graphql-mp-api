type Query {
    products: [Product!]!
    product(id: String!): Product
}

type Product {
    id: ID!
    title: String!
    description: String
    slug: String
    status: String
    category: Category
}

type Category {
    id: ID!
    title: String!
    description: String
    slug: String
}
